# NEW FILE ONLY — copy to application-local.properties (gitignored) for local dev.
# See application.properties for import paths.

spring.datasource.url=jdbc:postgresql://localhost:5432/planora
spring.datasource.username=
spring.datasource.password=

planora.cors.allowed-origins=http://localhost:5173
